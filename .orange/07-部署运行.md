# 07 部署运行

## 安装

```bash
pip install langgraph
pip install langgraph-checkpoint-postgres  # PostgreSQL 支持
pip install langgraph-sdk                  # LangGraph Platform 客户端
```

## 基础使用

```python
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from langchain_anthropic import ChatAnthropic

# 定义状态
from typing import TypedDict, Annotated
from langgraph.graph.message import add_messages

class State(TypedDict):
    messages: Annotated[list, add_messages]

# 构建图
model = ChatAnthropic(model="claude-sonnet-4-5")

def call_model(state: State):
    response = model.invoke(state["messages"])
    return {"messages": [response]}

builder = StateGraph(State)
builder.add_node("model", call_model)
builder.add_edge(START, "model")
builder.add_edge("model", END)

graph = builder.compile(checkpointer=InMemorySaver())

# 执行
config = {"configurable": {"thread_id": "thread-1"}}
result = await graph.ainvoke(
    {"messages": [{"role": "user", "content": "Hello"}]},
    config=config
)
```

## PostgreSQL 生产部署

```bash
# 环境变量
export POSTGRES_URI="postgresql://user:pass@localhost:5432/langgraph"
```

```python
from langgraph.checkpoint.postgres import PostgresSaver

async with PostgresSaver.from_conn_string(os.environ["POSTGRES_URI"]) as checkpointer:
    await checkpointer.setup()  # 创建表（首次运行）
    graph = builder.compile(checkpointer=checkpointer)
    # ...
```

## LangGraph Platform

LangGraph Platform 是 LangChain 公司的商业部署服务：

```yaml
# langgraph.json 配置
{
  "dependencies": ["."],
  "graphs": {
    "agent": "./agent.py:graph"
  },
  "env": ".env"
}
```

```bash
# 本地开发服务器
pip install langgraph-cli
langgraph dev

# 部署到 LangGraph Cloud
langgraph deploy
```

**LangGraph Platform 提供:**
- REST API（自动生成）
- 持久化存储（PostgreSQL managed）
- 流式传输（SSE）
- Studio UI（图可视化 + 调试）
- 调度器（Cron jobs）
- Webhooks

## Docker 部署

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "-m", "uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

```python
# app.py - FastAPI 集成
from fastapi import FastAPI
from langgraph.graph import StateGraph

app = FastAPI()
graph = build_graph()

@app.post("/invoke")
async def invoke(request: InvokeRequest):
    config = {"configurable": {"thread_id": request.thread_id}}
    result = await graph.ainvoke(request.input, config=config)
    return result
```

## 环境配置

```bash
# LangSmith 追踪（可选）
export LANGCHAIN_TRACING_V2=true
export LANGCHAIN_API_KEY=your_key
export LANGCHAIN_PROJECT=my-project

# Anthropic API
export ANTHROPIC_API_KEY=your_key

# OpenAI API
export OPENAI_API_KEY=your_key
```

## LangGraph Studio（本地调试）

LangGraph Studio 是 LangGraph 的可视化调试工具（桌面应用）：
- 实时查看图执行流
- 检查每个节点的输入/输出
- 修改状态并重新执行
- 查看 Checkpoint 历史

```bash
# macOS 安装
brew install langgraph-studio
# 或从 GitHub Releases 下载 .dmg
```
